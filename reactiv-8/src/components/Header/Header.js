import React from "react";
import PropTypes from "prop-types";
import Nav from "../Nav/Nav";

const Header = () => (
  <div>
    Header Component
    <Nav />
  </div>
);

Header.propTypes = {};

Header.defaultProps = {};

export default Header;
